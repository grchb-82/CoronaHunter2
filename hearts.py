import pygame

class Hearts5(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/5hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts46(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/46hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts43(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/43hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)
class Hearts4(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/4hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)


class Hearts36(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/36hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts33(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/33hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)
class Hearts3(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/3hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)


class Hearts26(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/26hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts23(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/23hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)
class Hearts2(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/2hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts16(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/16hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts13(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/13hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)
class Hearts1(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/1hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts06(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/06hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)

class Hearts03(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/03hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)
class Hearts0(pygame.sprite.Sprite):
    def __init__(self, x, y):
        super().__init__()
        self.image = pygame.image.load("assets/sprites/0hearts.png").convert_alpha()
        self.rect = self.image.get_rect()
        self.rect.topleft = (x, y)


